# CoPilot Custom Rules (demo)

Sample per-tenant detection rules for SOCFortress CoPilot's Detection Rule Editor.
Rules live under `detections/<folder>/<rule>.yaml`, same layout as the shared
`socfortress/CoPilot-Search-Queries` catalog. Graylog-only format.
